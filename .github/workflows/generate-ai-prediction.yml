name: Generate AI Prediction

on:
  schedule:
    # 每周一、三、五 UTC 00:00（北京时间 08:00）自动运行
    # 对应开奖后一天：周日开奖→周一，周二开奖→周三，周四开奖→周五
    - cron: '0 0 * * 1,3,5'
  workflow_dispatch: # 也支持手动触发

jobs:
  generate-prediction:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          persist-credentials: true

      - name: Set up Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install openai

      - name: Run statistical/ML models
        run: python3 -m strategies.runner

      - name: Run AI prediction generator (LLM models)
        run: python3 generate_ai_prediction.py
        env:
          AI_API_KEY: ${{ secrets.AI_API_KEY }}
          AI_BASE_URL: ${{ secrets.AI_BASE_URL }}

      - name: Check for changes
        id: check_changes
        run: |
          git diff --quiet data/ || echo "changed=true" >> $GITHUB_OUTPUT

      - name: Commit and push if changed
        if: steps.check_changes.outputs.changed == 'true'
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add data/ai_predictions.json data/predictions_history.json data/token_usage.json
          git commit -m "chore: generate AI predictions $(date +'%Y-%m-%d %H:%M:%S')"
          git push

      - name: Summary
        if: steps.check_changes.outputs.changed == 'true'
        run: |
          echo "✅ AI predictions generated successfully" >> $GITHUB_STEP_SUMMARY
          echo "Generated at: $(date)" >> $GITHUB_STEP_SUMMARY

      - name: No changes
        if: steps.check_changes.outputs.changed != 'true'
        run: |
          echo "ℹ️ No changes in prediction data" >> $GITHUB_STEP_SUMMARY
